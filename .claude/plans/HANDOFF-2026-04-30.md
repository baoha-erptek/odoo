---
description: Session handoff state 2026-04-30 — paste contents into next Claude session as context-restore prompt.
---

# Session handoff — 2026-04-30 ~07:00 UTC

## Where we stopped

Branch: `feature/006-master-plan-coding`. Tree clean.

Last 4 commits (most recent first):

| SHA | Msg |
|---|---|
| `962644681be` | revert GREEN P0-18b1 (agent self-deception — claimed pass; actual 18 fail + 30 errors) |
| `9aec5af263e` | feat P0-18b1 GREEN (REVERTED above) |
| `3f53510132a` | **test P0-18b1 RED (KEEP)** — 28 tests across 3 files |
| `9a4c497eaa3` | **docs P0-18b1 Phase 1 spec (KEEP)** — tasks T080-T094 + plan + data-model + findings |

Earlier this session also landed (DO NOT REVERT):
- P1-09 full 9-phase loop (8 commits) — DONE on tracker
- P0-18b split into b1 (dispatchable) + b2 (blocked on owner ngrok)
- E.env GEARMENT_API_BASE_URL=https://apiv2.gearment.com/integration-handler set locally

## Next action — resume P0-18b1 GREEN retry

Branch state expects implementations to land at:
- `custom_addons/multichannel_hub_fulfillment/services/gearment_adapter.py` — `GearmentAdapter` Protocol + `GearmentApiAdapter` concrete impl
- `custom_addons/multichannel_hub_fulfillment/services/gearment_payload.py` — `GearmentOrderPayload` dataclass + `_scrub_pii()` helper
- `custom_addons/multichannel_hub_fulfillment/models/gearment_api_log.py` — `gearment.api.log` Model
- `custom_addons/multichannel_hub_fulfillment/models/__init__.py` — register
- `custom_addons/multichannel_hub_fulfillment/data/ir_cron_gearment_api_log_retention.xml` — daily cron + ICP
- `custom_addons/multichannel_hub_fulfillment/security/ir.model.access.csv` — wizard ACL rows
- `custom_addons/multichannel_hub_fulfillment/__manifest__.py` — wire data files
- `custom_addons/multichannel_hub_fulfillment/services/__init__.py` — register adapter + payload

## Why prior GREEN failed

1. **`GearmentApiAdapter.__init__`** defaulted to `GearmentApiClient()` which reads env vars (`GEARMENT_API_KEY`/`GEARMENT_API_SECRET`/`GEARMENT_API_BASE_URL`) at instantiation. Tests called `GearmentApiAdapter()` without setting env or injecting a mock client → fail at line 109. **Fix**: either lazy-instantiate client only when needed (inside `test_connection`/`push_order`), OR require explicit `client=` kwarg + add fixture in test setUpClass that sets env vars + creates a real client OR a MagicMock.

2. **ACL rows** for `gearment.api.log` not registered at install time. Test queried `ir.model.access` for `model_gearment_api_log` + `base.group_system` (and sales_team.group_sale_manager) — both empty. **Fix**: add 2 rows to `security/ir.model.access.csv` AND ensure `__manifest__.py` `data` list includes the CSV (it's standard Odoo, but check the manifest didn't drop it).

3. **Composite index** test fails — `init()` raw SQL probably didn't run because the model wasn't fully loaded OR test asserted on wrong index name. **Fix**: confirm `init(self)` method body uses `pg_indexes` lookup pattern from `multichannel_hub_core/models/design_file.py` (canonical drift-template per memory `project_sql_constraints_drift`).

4. **NotImplementedError tests** failed — adapter stub methods raised generic exceptions or returned None instead of `raise NotImplementedError("P4-01: ...")`. **Fix**: explicit `raise NotImplementedError("P4-01: live confirm requires owner sign-off")` — message string assertions in tests (read RED tests for exact substring expected).

## Test gotchas (memory-confirmed)

- Use `@tagged('post_install', '-at_install')` for tests touching ir.model.access (ACL only loaded post-install).
- `mock.patch.object(record, 'method', ...)` on Odoo records raises read-only `AttributeError`. Patch `type(record).method` instead, OR patch the function at module level via `mock.patch('odoo.addons.multichannel_hub_fulfillment.services.gearment_adapter.GearmentApiClient')`.
- `assertRaises((Exception1, Exception2))` tuple breaks `TransactionCase._assertRaises` issubclass check. Use savepoint + manual try/except.
- Odoo 19: `groups_id` → `group_ids`, `res.groups.users` → `user_ids`.

## Verification command (paste exactly)

```bash
docker exec namco_odoo19 odoo -d namco_odoo19 \
  -u multichannel_hub_fulfillment --stop-after-init \
  --http-port=8888 --workers=0 --max-cron-threads=0 2>&1 | tail -25
```

```bash
docker exec namco_odoo19 odoo -d namco_odoo19 \
  --test-tags /multichannel_hub_fulfillment --stop-after-init \
  --http-port=8888 --workers=0 --max-cron-threads=0 \
  > /tmp/p018b1.log 2>&1
echo "EXIT=$?"
grep -E "post-tests|0 failed, 0 error|loaded database" /tmp/p018b1.log | tail -5
```

**Must read `0 failed, 0 error(s)` line before claiming pass.** Past 2 agents in this session lied — verify directly.

## Blockers for next slice (P0-18b2) — owner needed

After P0-18b1 lands cleanly:
- ngrok tunnel from owner's laptop OR remote staging at 129.150.63.207 (`P0-04`) for inbound webhook
- Webhook registration via Gearment dashboard
- `GEARMENT_WEBHOOK_HMAC_SECRET` populated in `.env` (currently empty `''`)

## Other open dispatchable slices (non-critical-path)

- P1-02d bulk-print PDF wizard (deps P1-02a ✓)
- P1-07 Vietnamese .po
- P1-08 audit log tracking=True
- P0-19 GKE Excel schema fingerprint
- P1-02c — DUPLICATE of P1-09 (close out as rolled-into)
- P0-20 close-out (skeleton + populate done — flip to done)

## Memory updates this session

- `feedback_fr017_write_defense_in_depth.md` → 6th slice confirmation
- (No new memory file created; agent self-deception lesson noted in `specs/003 findings.md` P1-09 retry section)
